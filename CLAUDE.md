# BiliMarketMonitor 开发约定

通用规范见全局 `~/.claude/CLAUDE.md`（`pixi run python`、提交格式、小工具管理），这里只记本项目自己的事。

## 改了界面就出图看一眼

对话框、表格、主题的样式改动，跑 `pixi run shot` 渲染成 PNG 确认，别只读代码下结论。
场景清单与用法见 [tools/README.md](tools/README.md)。

（全局那份只在"写新工具前"让人去看 `tools/`，管不到"改样式时"，所以这句得写在这儿。）
